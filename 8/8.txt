gcc -E    .i
gcc -S    .s
gcc -c    .o


链接：
	a.我们要清楚，我们自己写的代码，和库是两码事

	C标准库 是 别人已经给我们准备好的，让我们直接使用的 （已经编译好的二进制可执行代码）

	我们所有使用库中函数的代码（printf(...)），其中我们只写了该函数的调用，没有对应的实现
	只有当 链接 的时候，对应的实现，才和我们的代码关联起来

链接的本质：无非就是我们调用，库函数的时候，和标准库，如何关联的问腿


makefile、make

make 是一个命令， makefile是一个文件
	
	依赖关系 和 依赖方法

	makefile原理：
		makefile 存在的意义，是为了构建项目的（要做的一件事情）
			a.依赖关系
			b.依赖方法



accsee    访问文件的时间（老的内核代码，access只要访问时间就改，access时间被更改的太频繁了）    访问：cat 、
																	要进行更多次的IO
															新内核的修正，一段时间内访问次数够了，才更改一次时间
modify    修改文件内容的时间
change    修改文件属性的时间（文件大小也属于文件时间）


gcc or makefile
	要看.c 文件的modify时间，是否比 可执行程序的modify时间新
	新了就编译，旧就不编译


touch 除了创建一个不存在的文件，还可以将存在的文件的时间更新

.PHONY 的作用就是不根据 时间来判断

默认情况下makefile只形成一个可执行文件，后去的依赖方法再不执行

makefile 很像一个栈 先来的要后走 